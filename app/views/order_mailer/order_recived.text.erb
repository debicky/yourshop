<h1>Thanks <%=  %> for your order! </h1>

You bought following items:
<hr>
    <% @checkout.order_items.each do |x| %>
    <%= x.quantity %> x
    <%= x.product.title %> | 
    <%= x.product.price %> $ |size: 
    <%= x.size.size %> |
    <hr>
    <% end %>

    Total order value: <%= @checkout.order_items.collect {|order_item| order_item.total}.sum %> $

