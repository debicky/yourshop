<nav class="navbar navbar-expand-lg navbar-light border-bottom mb-3 py-5 ">
  <%= link_to "YourShop", root_path, class: "btn btn-outline-secondary btn-lg shadow-lg" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if current_user.try(:admin?) %>
      <li class="nav-item">
        <%= link_to 'New Product', new_product_path, class: "nav-link btn btn-warning btn-sm mx-1" %>
      </li>
      <li class="nav-item">
        <%= link_to "New Categories", categories_path, class: "nav-link btn btn-warning btn-sm mr-5" %>
      </li>
      <% end %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= current_user ? current_user.email.split("@")[0] : "User" %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>

            <%= link_to('Edit registration', edit_user_registration_path, class: "dropdown-item") %>
            <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "dropdown-item") %>        

          <% else %>

            <%= link_to('Register', new_user_registration_path, class: "dropdown-item")  %>
            <%= link_to('Login', new_user_session_path, class: "dropdown-item")  %>  

          <% end %>
        </div>
      </li>
    </ul>

  </div>
</nav>